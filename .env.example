# Local-only environment template (do not commit real values).
# This file documents keys used by upload-build workflow.

EXPORT_OPTIONS="<raw ExportOptions.plist contents>"
APPLE_API_KEY="<base64 encoded App Store Connect API key .p8>"
APPLE_API_KEY_ID="<App Store Connect API key id>"
APPLE_API_ISSUER_ID="<App Store Connect issuer id>"
APPLE_ID="<Apple account email>"
APP_SPECIFIC_PASSWORD="<App-specific password>"
